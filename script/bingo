
const cartonsSolded = "";
const columnsNumber = 9;
const rowsNumber= 3;
const maxNumbersRows= 5;
const minNumber = 1;
const maxNumber = 90;
const tryings = (maxNumber-minNumber+1);

for(i=minNumber;i<=((maxNumber-minNumber)/rowsNumber+1);i++){
numbersDistributionRows = [[],[],[]];
numbersDistributionRows.push()


}

for (i=minNumber;i<=maxNumber;i++){
  const ballContainer = [];
  ballContainer.push(i);
}


for(x=1;x<=cartonsSolded;x++){
const cartonsOnGame=[];
const cartonCreator =[
  for(y=1;y<=rowsNumber;y++){
    for(z=1;z<=columnsNumber;z++){


    }


  }


]
cartonNumbers.push();


}


for (i=minNumber;i<=maxNumber;i++){
  const ballContainer = [];
  ballContainer.push(i);
  shuffle(ballContainer);
}

function ballContainerOutput () {
  const randomNumber= Math.floor(Math.random * ballContainer[ballContainer.length-1]);
  ballContainer.pop();
  shuffle(ballContainer);
  console.log(randomNumber);
}

for(i=tryings;(ballContainer.length>0)&&(bingo=false);i--){
ballContainerOutput()
}





